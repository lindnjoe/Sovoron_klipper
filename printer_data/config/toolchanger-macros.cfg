# M104/M109/SET_TEMPERATURE_WITH_DEADBAND macros removed.
# AFC now handles M104/M109 natively with auto-deadband support.
# Set deadband_auto: True in [AFC] config (default) to automatically
# use each extruder's configured deadband value for M109 wait commands.
# Manual deadband can still be passed via M109 D=<value>.
